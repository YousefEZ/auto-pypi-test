[bumpver]
current_version = "6.0.3"
version_pattern = "MAJOR.MINOR.PATCH"
commit = true
push = true
tag = true

[bumpver.file_patterns]
"setup.py" = [
    'version="{version}"',
]

"bumpver.toml" = [
    'current_version = "{version}"'
]
